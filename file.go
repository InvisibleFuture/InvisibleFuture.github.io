package main

type File []byte
/**
func (f File)Create(name, size, hash, time, path []byte){
	FILE_NAME_DB.Put(f, name, nil)
	FILE_SIZE_DB.Put(f, size, nil)
	FILE_TIME_DB.Put(f, time, nil)
	FILE_PATH_DB.Put(f, path, nil)
	FILE_HASH_DB.Put(f, path, nil)
}

type attach struct {
	Name string
	Path string
	Size string
	Hash string
	Time string
}
**/
